{
  "name": "symmetry-news-backend-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test:rules": "firebase emulators:exec --project demo-symmetry-news --only firestore,storage \"npm run test:rules:local\"",
    "test:rules:local": "node --test tests/firestore.rules.test.js tests/storage.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^10.14.1",
    "firebase-tools": "^14.24.0"
  }
}
